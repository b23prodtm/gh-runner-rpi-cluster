# x86_64.env
# Optional x86_64 host (for example an Intel NUC) used as extra build capacity
# or for local development/testing of the same balena deployment layout.

BALENA_ARCH=x86_64
PLATFORM=linux/amd64
PRIMARY_HUB=ubuntu
PRIMARY_TAG=22.04
RUNNER_ARCH=x64
