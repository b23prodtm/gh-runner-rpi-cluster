# aarch64.env
# Raspberry Pi 4 / 5 running balenaOS in 64-bit mode.
# File name matches $(BALENA_ARCH) so update_templates can pick it
# automatically alongside common.env.

BALENA_ARCH=aarch64
PLATFORM=linux/arm64
PRIMARY_HUB=ubuntu
PRIMARY_TAG=22.04
RUNNER_ARCH=arm64
